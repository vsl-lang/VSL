/**
 * Handles color for FixIt
 */
export default class FixItColors {
    accent(text) {
        throw new TypeError(`no accent color`);
    }
    
    color(text) {
        throw new TypeError(`no basic color`);
    }
}
