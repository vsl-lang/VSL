@primitive(Integer, _precType)
@_mockType(llvm, "i32", signed)
public class Int {

    @backend(llvm)
    public static func +(lhs: Int, rhs: Int) -> Int native {
        ret i32 ( add i32 %1, %2 )
    }
    
}
