public interface Equatable {
    @inline
    static func ==(lhs: Self, rhs: Self)
    
    static func !=(lhs: Self, rhs: Self)
    
    @inline
    @backend(llvm)
    static func !=(lhs: Self, rhs: Self) default native {
        %2 = call i1 @"<root>.=="(<param> %0, <param> %1)
        %3 = icmp eq i1 0, %2
        ret %3
    }
    
    @inline
    @backend(js)
    static func !=(lhs: Self, rhs: Self) default native {
        return !<root>["=="](lhs, rhs);
    }
}
