public interface Equatable {
    static func ==(lhs: Self, rhs: Self)
    static func !=(lhs: Self, rhs: Self) llvm {
    define i1 <name>(<param>, <param>) alwaysinline {
    entry:
        %2 = call i1 @"<methodRoot>.=="(<param> %0, <param> %1)
        %3 = icmp eq i1 0, %2
        ret %3
    }
    }
}