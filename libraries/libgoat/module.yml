name: libgoat
description: Goat library. This is a test/example library.

version: 1.0.0

# This also can function as a library but it has an executable,. Meaning you can
# override this behavior through a `library: ` command in the module.yml or
# through command line flags.
target: executable

sources:
    - src/**/*.vsl
