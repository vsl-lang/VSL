func main() {
    let x: Int = 1
    let y: Int = 2
    assert(x <= y)

    let a: Int = 1
    let b: Int = 1
    assert(a <= b)

    let c: UInt = 1
    let d: UInt = 2
    assert(c <= d)

    let e: UInt = 1
    let f: UInt = 1
    assert(e <= f)
}
